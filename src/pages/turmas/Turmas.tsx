import { AppToolbar } from '../../shared/components';
import { LayoutBase } from '../../shared/layouts';

export const Turmas = () => {

  return (
    <LayoutBase
      AppToolbar={(
        <AppToolbar />
      )}
    >
      Em breve...
    </LayoutBase>
  );
};
